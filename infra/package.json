{
  "name": "sportlink-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Pulumi IaC for the SportLink backend (EC2 + S3 + IAM)",
  "main": "index.ts",
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@pulumi/aws": "^6.0.0",
    "@pulumi/pulumi": "^3.0.0"
  }
}
